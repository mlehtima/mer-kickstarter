Install cheetah (http://www.cheetahtemplate.org/) templating system, PyYAML.

To install run:

$ make
$ sudo make install

To generate kickstarts run:
$ kickstarter -c <configuration.yaml> -o <outdir>

As a reference for configurations see following packages:
- mer-kickstarter-configs
- sdk-kickstarter-configs

